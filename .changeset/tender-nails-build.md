---
"@web-react-player/core": minor
"@web-react-player/ui": minor
---

Add per-video quality selection (VideoQuality, SET_QUALITIES/QUALITY_CHANGE/LOAD.qualities, seamless source swap via `actions.setQuality`) and adaptive responsive tiers (ContainerTier, `getContainerTier`, `tier` in PlayerContext) with a unified SettingsMenu consolidating quality, speed, ambient, document PiP and caption styles, plus an adaptive compact Skip pill for narrow containers